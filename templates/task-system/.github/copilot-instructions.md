# Shared task system

Follow the `TASKS/` workflow in the repository's `AGENTS.md` before changing
code. Search for related work first, mark the chosen task `in_progress`, and
append dated Agent Notes as work progresses. Do not mark a task done until the
requested work and its relevant validation are complete.
