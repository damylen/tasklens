# TaskLens workflow

Follow `AGENTS.md` before changing code. It points to the single canonical
task-system implementation under `templates/task-system/`; do not recreate the
shared workflow in this root instruction file.
