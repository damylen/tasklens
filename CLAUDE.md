# TaskLens workflow

Read and follow `AGENTS.md` before starting work. It points to the single
canonical task-system implementation under `templates/task-system/`; the root
skill files are discovery forwarders, not separate workflow copies.
